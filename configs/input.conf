# Input Device Configuration
# Optimized input settings for productivity

input {
    kb_layout = us
    kb_variant = 
    kb_model = 
    kb_rules = 
    
    follow_mouse = 1
    focus_follows_mouse = true
    
    touchpad {
        natural_scroll = false
        tap-to-click = true
        disable_while_typing = true
        sensitivity = 0
        accel_profile = flat
        scroll_factor = 1.0
    }
}

# Cursor configuration
cursor {
    no_warps = false
    inactive_timeout = 0
}
